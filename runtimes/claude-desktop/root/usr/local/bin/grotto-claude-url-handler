#!/bin/bash
set -Eeuo pipefail

uri="${1:-}"
case "$uri" in
    claude://*) exec /usr/bin/claude-desktop "$uri" ;;
    *)
        printf 'Unsupported Claude Desktop URL: %s\n' "$uri" >&2
        exit 64
        ;;
esac
