# openquad-browser-runtime-headless intentionally does not use Homebrew.
# It is a browser runtime image, not an OpenClaw agent appliance.
# Browser dependencies come from the Playwright runtime base image.
